{
  "name": "qtdn",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --build --force && tsc --noEmit -p apps/mobile && tsc --noEmit -p e2e",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run lint && npm run typecheck && npm run test:coverage",
    "check:all": "npm run check && npm run latency && npm run e2e",
    "build:ios": "npm run export --workspace @qtdn/mobile",
    "test:coverage": "vitest run --coverage",
    "e2e": "npm run build:web --workspace @qtdn/mobile && playwright test --config e2e/playwright.config.ts",
    "e2e:report": "playwright show-report",
    "latency": "vite-node e2e/latency.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.9.0",
    "@vitest/coverage-v8": "^2.1.9",
    "eslint": "^9.15.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "fast-check": "^3.23.1",
    "globals": "^15.15.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0",
    "vite-node": "^6.0.0",
    "vitest": "^2.1.5"
  }
}
